

---

# 线缆点位测试表转换软件 MVP Spec

## 1. 项目背景与目标

线缆点位测试表转换软件用于将人工线缆接线表转换为测试仪可识别的标准编程表。

MVP 版本目标是打通最小可用流程：

```text
上传 Excel
  ↓
解析表格结构
  ↓
识别关键字段
  ↓
转换为编程表
  ↓
下载结果 ExcelMVP 重点验证：
```

- Excel 文件能否被正确读取；
- 起点/终点插件和针脚能否被识别；
- 合并单元格能否正确处理；
- 连接器名称和针脚能否标准化；
- 是否能生成接近样例的测试仪编程表。

---

## 2. MVP 功能范围

### 2.1 本阶段实现

| 模块       | 功能                                             |
| ---------- | ------------------------------------------------ |
| 文件上传   | 支持上传 `.xlsx` 文件                            |
| Excel 解析 | 读取 Sheet，识别有效表格区域                     |
| 字段识别   | 识别起点插件、起点针脚、终点插件、终点针脚等字段 |
| 数据清洗   | 处理空行、合并单元格、前后空格                   |
| 规则转换   | 标准化连接器名称，拆分针脚，生成 Net/Sub 编号    |
| 数据预览   | 展示原始解析数据和转换结果预览                   |
| Excel 导出 | 生成 `.xlsx` 编程表文件                          |
| 文件下载   | 用户下载转换结果                                 |

### 2.2 本阶段暂不实现

- 用户登录和权限管理；
- 数据库存储和历史记录；
- 在线编辑 Excel；
- 复杂规则配置后台；
- 完整桌面端打包；
- 所有特殊线缆类型的完整规则覆盖。

---

## 3. 输入与输出说明

### 3.1 输入文件

参考输入文件：

```text
【CX-6(09)】低频电缆网#投产-最后.xlsx
```

输入表为人工线缆接线表，主要结构如下：

| 列   | 字段     | 说明                         | 示例                       |
| ---- | -------- | ---------------------------- | -------------------------- |
| A    | 起点插件 | 起点连接器，可能是合并单元格 | `PW01-X40(J30J-74TJSL7)`   |
| B    | 起点针脚 | 起点针脚，可能包含多个值     | `45,63`                    |
| C    | 起点内容 | 起点信号说明                 | `电推控制器电源开指令`     |
| D    | 终点插件 | 终点连接器，可能是合并单元格 | `PK09-X01(J30J-51ZKSL7)`   |
| E    | 终点针脚 | 终点针脚，可能包含多个值     | `1,2`                      |
| F    | 终点内容 | 终点信号说明                 | `控制器电源开指令`         |
| G    | 信号性质 | 信号规格或性质               | `26#`                      |
| H    | 备注     | 特殊说明                     | `接地片`、`双绞`、`接外壳` |

输入特点：

- 表头不一定在第一行；
- 顶部存在说明区；
- 起点/终点插件可能使用合并单元格；
- 针脚可能用逗号分隔多个值；
- 备注列可能存在特殊标记。

### 3.2 输出文件

参考输出文件：

```text
【CX-6(09)】低频电缆网#投产-最后编程表.xlsx
```

输出文件为测试仪编程表，支持多个 Sheet。

输出命名规则：

```text
原文件名 + 编程表.xlsx
```

输出表基础结构：

| 列   | 字段     | 示例                                   |
| ---- | -------- | -------------------------------------- |
| A    | Net 编号 | `Net1`                                 |
| B    | Sub 编号 | `*Sub 1`                               |
| C/D  | 连接关系 | `PY01_X04::PY03_X02:2` 或 `PY01_X05:2` |

示例：

```text
Net1    *Sub 1    PY01_X04::PY03_X02:2
Net2    *Sub 2    PY01_X04::PY03_X02:3
Net3    *Sub 3    PY01_X04::PY03_X02:4
```

---

## 4. 核心转换规则

### 4.1 有效数据识别

系统需要自动跳过顶部说明区，识别包含以下字段的表头行：

```text
起点插件、终点插件、内容、信号性质、备注
```

表头之后的数据作为候选接线数据。空行、无起点/终点信息的行应跳过。

### 4.2 合并单元格处理

起点插件、终点插件可能为合并单元格。系统需要将合并单元格的值向下填充，使每一行都有完整的起点和终点信息。

示例：

```text
A6:A12 = PW01-X40(J30J-74TJSL7)

处理后：
A6 = PW01-X40(J30J-74TJSL7)
A7 = PW01-X40(J30J-74TJSL7)
...
```

### 4.3 连接器名称标准化

输入连接器名称需要转换为输出标准格式。

规则：

1. 去掉括号及括号内型号；
2. 将 `-` 替换为 `_`；
3. 去除空格和换行。

示例：

| 输入                        | 输出            |
| --------------------------- | --------------- |
| `PW01-X40(J30J-74TJSL7)`    | `PW01_X40`      |
| `PK09-X01(J30J-51ZKSL7)`    | `PK09_X01`      |
| `ZJ11-PK09-X01(J30J-9ZKSP)` | `ZJ11_PK09_X01` |

### 4.4 针脚拆分

针脚字段可能包含多个值，需要按逗号拆分。

示例：

```text
45,63 → 45 / 63
1,2 → 1 / 2
```

支持：

- 英文逗号 `,`
- 中文逗号 `，`

针脚号按字符串处理，避免被 Excel 或 pandas 转成浮点数。

### 4.5 Net/Sub 编号

每条输出记录自动生成编号：

```text
第 1 条：Net1，*Sub 1
第 2 条：Net2，*Sub 2
第 3 条：Net3，*Sub 3
```

每个 Sheet 内从 1 开始重新编号。

### 4.6 特殊备注处理

备注中可能出现：

```text
接地片、接外壳、双绞
```

MVP 阶段先识别并在预览中提示。  
如果暂时无法转换，不直接丢弃，应作为警告展示。

---

## 5. 页面原型

前端采用：

```text
Vue3 + Element Plus
```

MVP 包含 3 个页面。

### 5.1 上传页面

功能：

- 上传 Excel；
- 显示文件名；
- 点击开始解析。

草图：

```text
+--------------------------------------+
| 线缆点位测试表转换软件               |
+--------------------------------------+
|                                      |
|      拖拽或点击上传 Excel 文件        |
|                                      |
|      支持格式：.xlsx                 |
|                                      |
|      [ 开始解析 ]                    |
+--------------------------------------+
```

### 5.2 解析预览页面

功能：

- 展示文件名、Sheet 信息；
- 展示字段识别结果；
- 展示原始解析预览；
- 展示转换结果预览；
- 确认转换。

草图：

```text
+--------------------------------------+
| 解析预览                             |
+--------------------------------------+
| 文件名：xxx.xlsx                     |
| 有效行数：xxx                        |
| 警告数量：x                          |
+--------------------------------------+
| 字段识别结果                         |
| 起点插件 -> A列                      |
| 起点针脚 -> B列                      |
| 终点插件 -> D列                      |
| 终点针脚 -> E列                      |
+--------------------------------------+
| 原始数据预览                         |
| 转换结果预览                         |
+--------------------------------------+
| [ 返回 ]              [ 确认转换 ]   |
+--------------------------------------+
```

### 5.3 转换完成页面

功能：

- 展示转换成功；
- 展示输出文件名；
- 下载结果；
- 重新上传。

草图：

```text
+--------------------------------------+
| 转换完成                             |
+--------------------------------------+
|            转换成功                  |
| 输出文件：xxx编程表.xlsx             |
|                                      |
| [ 下载结果 ]                         |
| [ 重新上传 ]                         |
+--------------------------------------+
```

---

## 6. 接口设计

后端采用：

```text
Python + Flask + pandas + openpyxl
```

### 6.1 健康检查

```http
GET /api/health
```

返回：

```json
{
  "status": "ok"
}
```

### 6.2 上传并解析

```http
POST /api/files/upload
```

请求：

```text
multipart/form-data
file: Excel 文件
```

返回示例：

```json
{
  "file_id": "temp_001",
  "filename": "【CX-6(09)】低频电缆网#投产-最后.xlsx",
  "sheets": [
    {
      "sheet_name": "Sheet1",
      "header_row": 5,
      "row_count": 120,
      "matched_columns": {
        "start_connector": "A",
        "start_pin": "B",
        "start_content": "C",
        "end_connector": "D",
        "end_pin": "E",
        "end_content": "F",
        "signal_type": "G",
        "remark": "H"
      },
      "source_preview_rows": [],
      "converted_preview_rows": []
    }
  ],
  "warnings": [],
  "errors": []
}
```

### 6.3 确认转换

```http
POST /api/files/convert
```

请求：

```json
{
  "file_id": "temp_001"
}
```

返回：

```json
{
  "success": true,
  "output_file_id": "output_001",
  "output_filename": "【CX-6(09)】低频电缆网#投产-最后编程表.xlsx"
}
```

### 6.4 下载结果

```http
GET /api/files/download/{output_file_id}
```

---

## 7. 项目结构

推荐项目结构：

```text
cable-point-converter/
├── README.md
├── docs/
│   ├── spec.md
│   └── prototype/
│       └── cable-point-converter.epgz
├── backend/
│   ├── app.py
│   ├── requirements.txt
│   ├── uploads/
│   ├── outputs/
│   └── services/
│       ├── excel_reader.py
│       ├── merged_cell_handler.py
│       ├── column_detector.py
│       ├── connector_normalizer.py
│       ├── pin_parser.py
│       ├── cable_converter.py
│       └── excel_writer.py
└── frontend/
    ├── package.json
    ├── vite.config.js
    └── src/
        ├── main.js
        ├── App.vue
        ├── api/
        │   └── fileApi.js
        └── views/
            ├── UploadView.vue
            ├── PreviewView.vue
            └── ResultView.vue
```

模块说明：

| 模块                 | 说明             |
| -------------------- | ---------------- |
| excel_reader         | 读取 Excel       |
| merged_cell_handler  | 处理合并单元格   |
| column_detector      | 识别表头和字段   |
| connector_normalizer | 标准化连接器名称 |
| pin_parser           | 拆分针脚         |
| cable_converter      | 转换为编程表数据 |
| excel_writer         | 导出 Excel       |

---

## 8. 异常处理与校验

### 8.1 异常处理

| 场景             | 处理方式               |
| ---------------- | ---------------------- |
| 文件格式不支持   | 提示仅支持 Excel       |
| 文件为空         | 提示无有效内容         |
| Excel 无法读取   | 提示文件损坏或格式异常 |
| 未识别到表头     | 提示缺少关键字段       |
| 未识别到有效数据 | 提示无可转换数据       |
| 存在特殊备注     | 显示警告               |
| 转换失败         | 返回失败原因           |
| 下载文件不存在   | 提示文件不存在或已过期 |

### 8.2 基础校验

- 起点插件不能为空；
- 终点插件不能为空；
- 起点针脚或终点针脚至少有一个不能为空；
- 空行自动跳过；
- 插件名称标准化后不能为空；
- 特殊备注行不直接丢弃，应提示用户。

---

## 9. 交付计划与验收标准

### 9.1 当前阶段交付物

本阶段需要完成：

```text
1. docs/spec.md
2. Pencil 原型文件
3. 前后端项目初始化
4. README.md
```

### 9.2 计划

| 时间                     | 内容                                                         |
| ------------------------ | ------------------------------------------------------------ |
| 2026-04-30 至 2026-05-10 | 完成 Spec、Pencil 原型、项目初始化、基础导入导出、字段识别、核心转换、前端预览 |
| 2026-05-10 至 2026-05-13 | 验证、测试、微调、准备交付                                   |

### 9.3 MVP 验收标准

| 类型       | 标准                                         |
| ---------- | -------------------------------------------- |
| 文档       | `docs/spec.md` 完成                          |
| 原型       | 上传页、解析预览页、转换完成页完成           |
| 项目初始化 | Flask 后端和 Vue3 前端项目骨架完成           |
| 后端       | `/api/health` 可访问                         |
| 前端       | 项目可启动                                   |
| 功能闭环   | 能描述并支撑上传、解析、预览、转换、下载流程 |

---

## 10. 待确认问题

1. MVP 是否只支持 `.xlsx`，还是必须支持 `.xls`？
2. 输入文件是否可能包含多个有效 Sheet？
3. 输出 Sheet 的拆分规则是什么？
4. 输出 Sheet 名称是否必须与参考输出完全一致？
5. 起点针脚和终点针脚均为多个时，是一一对应还是全部展开？
6. `DMTL_X01` 等特殊连接器名称来源是什么？
7. `接地片`、`接外壳` 是否需要特殊输出格式？
8. `双绞` 是否只作为备注，还是影响输出分组？
9. 输出文件是否必须保留参考文件的样式、列宽、字体？
10. 后续是否需要封装为 Windows 桌面端？