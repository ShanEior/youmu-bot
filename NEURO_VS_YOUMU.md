# Neuro-sama vs 妖梦 Bot 对比分析

## 架构对比

| 维度 | Neuro-sama | 妖梦 Bot |
|------|-----------|----------|
| **运行平台** | Twitch 直播 | 微信 (ilink 协议) |
| **LLM** | 自研本地模型 (非 API 调用) | DeepSeek v4-flash (API) |
| **TTS** | Azure "Ashley" 音色 (+25% 音调) | 无 (纯文字) |
| **视觉** | 视觉 AI 看游戏画面 → 文字 | DeepSeek v4-flash vision (图片理解) |
| **部署** | C# (Unity Live2D) + Python (AI) | Docker 容器 (Python) |
| **框架** | 自研多模块协作 | CowAgent (chatgpt-on-wechat) |
| **载体** | Live2D 虚拟形象 | 微信聊天窗口 |

## 核心系统对比

### 1. 主动对话

| | Neuro-sama | 妖梦 Bot |
|------|-----------|----------|
| **触发方式** | 多源事件流持续输入 → LLM 自主判断 | 每 3 分钟心跳 → LLM 决策 SILENT/SPEAK |
| **信息源** | 聊天弹幕、游戏画面、合作者语音、直播事件 | 最近聊天内容、沉默时长、时间段 |
| **安静机制** | `silent` 参数：true 时只看不说 | 门控检查：5 分钟内活跃跳过、8 分钟冷却 |
| **夜间处理** | 根据直播时段调整活跃度 | Night 模式 (22-06) 不主动说话 |
| **成本模型** | 本地运行，无边际成本 | 决策 API 约 $0.00006/次，活跃期每日 ~$0.02 |

**妖梦实现方式**：
- `perception_heartbeat.py` — 持续感知心跳脚本
- cron 每 3 分钟触发
- 收集上下文 → DeepSeek 决策 → 写入 tasks.json → 调度器执行

### 2. 记忆系统

| | Neuro-sama | 妖梦 Bot |
|------|-----------|----------|
| **短期记忆** | 最近 N 次对话上下文 | `agent_max_context_turns: 20`，最多 50000 tokens |
| **长期记忆** | 向量库存储与检索 | MEMORY.md + memory/ 每日摘要 + knowledge/ 知识库 |
| **梦境蒸馏** | 直播间事件总结 | 每日对话摘要写入 memory/YYYY-MM-DD.md |
| **记忆调用** | LLM 接收向量检索结果融入回复 | Agent 读取 MEMORY.md，自然融入对话 |
| **持久化** | 服务重启不丢失 | 文件存储，卷挂载持久化 |

**妖梦实现方式**：
- CowAgent 内置 MemoryManager（无 embedding，使用关键词搜索）
- MEMORY.md 文件存储用户事实
- AGENT.md 中明确"记忆使用"规则：自然引用，不说"我记得"

### 3. 多模态

| | Neuro-sama | 妖梦 Bot |
|------|-----------|----------|
| **输入** | 视觉 AI (游戏画面→文字)、语音识别 | 微信图片消息 → DeepSeek vision |
| **输出** | TTS 语音 + Live2D 动作表情 | 文字 + GIF 表情包 (send 工具) |
| **表情系统** | 声调/模型控制 AI 映射到表情动作 | 13 种东方 Project 表情包 GIF |
| **看图能力** | 实时游戏画面分析 | 收到图片 → 描述 + 角色化吐槽 |

**妖梦实现方式**：
- 图片消息触发 `_send_image` → Agent vision 模式
- AGENT.md 看图规则：诚实描述 + 妖梦语气 + 情绪表情包
- 表情包映射：元气→卖萌唱歌.gif，害羞→呆住.gif，吐槽→疑问.gif 等

### 4. Actions 系统

| | Neuro-sama | 妖梦 Bot |
|------|-----------|----------|
| **游戏行为** | 注册 Action → LLM 自主选择执行 | 无游戏交互 |
| **工具调用** | SDK Force Actions / 自主 Action | Agent `send` 工具（表情包）、知识库检索 |
| **自主决策** | 可在任何时机主动执行 Action | 仅限对话中通过 Agent 工具调用 |
| **调度任务** | 无定时调度概念 | 7 个 cron 定时任务 + 感知心跳 + 静默检测 |

**妖梦实现方式**：
- 定时任务（早安 7:23、晚安 22:15 等 7 个时间点）
- 静默检测（用户沉默 30-60 分钟后随机 check-in）
- 感知心跳（LLM 自主决策，不固定时间）

### 5. 多人/多智能体

| | Neuro-sama | 妖梦 Bot |
|------|-----------|----------|
| **多虚拟人** | Event Queue → 分发策略 → 多 Agent 并行 | 无（单一角色） |
| **防冲突** | Task Queue 避免多人同时说话 | 单用户单线程，无冲突问题 |
| **合作互动** | Neuro + Evil 双 AI 对话 | 仅与用户一对一聊天 |

---

## 妖梦特有功能

### 上下文感知静默检测 (SilenceTrigger v2.0)
- **Normal 模式**：沉默 30-60 分钟 → 随机概率触发关心消息
- **Nap 模式**：用户说午睡 → 等 2-2.5 小时 → wake-up check-in
- **Night 模式**：用户晚上说晚安 → 完全不打扰到次日 7:00
- **Wake 检测**：用户说"醒了/起来了" → 立即恢复 Normal

### 标点自动过滤
- `_clean_sentence` 在代码层面 strip 结尾逗号
- 每句消息发送前自动清理，100% 可靠

### 文本+表情包双发
- AgentBridge bug 修复：`send` 工具发送表情包时文本被丢弃
- 修改 weixin_channel `send` 方法：检测 `text_content` → 先发文字 → 0.3s 后发图

### context_token 持久化
- WeChat ilink 推送需要 context_token
- 保存到 JSON 文件，容器重启自动加载
- 避免重启后无法主动发消息

### 会话主动性优化
- AGENT.md 中明确"聊天是双向的"
- 每轮回复至少一条反问或分享
- 冷场了主动找话题

---

## 未实现 / 待改进

| 功能 | 状态 |
|------|------|
| 本地 LLM 部署（降低延迟和成本） | 未实现 |
| TTS 语音输出 | 微信不支持 |
| 向量数据库长期记忆 | MemoryManager 无 embedding |
| 多用户群聊感知 | 仅单聊 |
| 更细粒度的情绪状态机 | 依赖 Prompt 指令，无代码层状态机 |
| Streaming 持续感知（非轮询） | 心跳轮询模式，非真正持续流 |

---

## 成本分析

| 项目 | 妖梦 Bot (日估) |
|------|----------------|
| 感知决策 API 调用 | ~30-50 次 × $0.00006 = ~$0.003 |
| 用户回复生成 | ~20 次 × $0.001 = ~$0.02 |
| 定时任务执行 | 7 次 × $0.001 = ~$0.007 |
| **日均总计** | **~$0.03** |
